mutation UpdateUser($data: UpdateUserDto!) {
    updateUser(data: $data) {
        id
        loginId
        nickname
        role
        avatar
        email
        createDate
        updateDate
    }
}